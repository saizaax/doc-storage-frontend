import React from "react"
import { ColorScheme, rem } from "@mantine/core"

export function Logo({ colorScheme }: { colorScheme: ColorScheme }) {
  return <img src="/Logo.png" height={28} alt="logo" />
}
